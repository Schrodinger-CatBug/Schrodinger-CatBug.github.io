{
  "name": "@daily-meal/site",
  "private": true,
  "version": "0.0.0",
  "description": "daily-meal 的 GitHub Pages 菜谱站点（Schrodinger-CatBug.github.io）",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --outDir ."
  },
  "dependencies": {
    "vue": "^3.5.38"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "vite": "^8.1.0"
  }
}
